# Core
pydantic>=2.0
pytest>=8.0
ortools>=9.10          # CP-SAT solver, Apache-2.0

# Criticality scoring (Phase 3)
lightgbm>=4.0
scikit-learn>=1.4
numpy>=1.26

# API + UI (Phase 4). The front end has no JS dependencies at all:
# plain JS and hand-drawn SVG, so a demo cannot fail on a slow CDN.
fastapi>=0.110
uvicorn>=0.29
