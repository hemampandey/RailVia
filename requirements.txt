# Core data model and solver
pydantic>=2.0
ortools>=9.10          # CP-SAT solver, Apache-2.0

# Criticality scoring
lightgbm>=4.0
scikit-learn>=1.4
numpy>=1.26

# API
fastapi>=0.110
uvicorn>=0.29

# Decisions are stored in Supabase; PyJWT verifies the sign-in tokens.
#
# [crypto] is NOT optional here: Supabase signs with ES256, and PyJWT can
# only verify asymmetric algorithms with `cryptography` installed. Plain
# PyJWT imports fine and then rejects every real token.
supabase>=2.0
PyJWT[crypto]>=2.8
cryptography>=42.0

# Testing
pytest>=8.0
httpx>=0.27            # required by fastapi.testclient
